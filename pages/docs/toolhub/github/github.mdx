# Github Toolkit

**Description:** AI tools for interacting with GitHub

## SetStarred

Star or un-star a GitHub repository.

For example, to star microsoft/vscode, you would use:
```
set_starred(owner="microsoft", name="vscode", starred=True)
```

### **Parameters:**

**owner**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">string</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
The owner of the repository
<hr style={{borderTop: '0.5px solid #888', margin: '10px 0'}} />
**name**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">string</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
The name of the repository
<hr style={{borderTop: '0.5px solid #888', margin: '10px 0'}} />
**starred**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">boolean</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
Whether to star the repository or not

---

## SearchIssues

Search for issues in a GitHub repository.

### **Parameters:**

**owner**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">string</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
The owner of the repository
<hr style={{borderTop: '0.5px solid #888', margin: '10px 0'}} />
**name**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">string</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
The name of the repository
<hr style={{borderTop: '0.5px solid #888', margin: '10px 0'}} />
**query**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">string</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
The query to search for
<hr style={{borderTop: '0.5px solid #888', margin: '10px 0'}} />
**limit**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">integer</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-optional">Optional</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-default">Defaults to 10</span>  
The maximum number of issues to return

---

## CountStargazers

Count the number of stargazers (stars) for a public GitHub repository.

For example, to count the number of stars for microsoft/vscode, you would use:
```
count_stargazers(owner="microsoft", name="vscode")
```

### **Parameters:**

**owner**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">string</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
The owner of the repository
<hr style={{borderTop: '0.5px solid #888', margin: '10px 0'}} />
**name**&nbsp;&nbsp;&nbsp;<span className="tool-param-type">string</span>&nbsp;&nbsp;&nbsp;<span className="tool-param-required">Required</span>&nbsp;&nbsp;&nbsp;  
The name of the repository

---

