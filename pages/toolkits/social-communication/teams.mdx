# Teams

import ToolInfo from "@/components/ToolInfo";
import Badges from "@/components/Badges";
import TabbedCodeBlock from "@/components/TabbedCodeBlock";
import TableOfContents from "@/components/TableOfContents";
import ToolFooter from "@/components/ToolFooter";

<ToolInfo
  description="Enable agents to interact with Teams"
  author="Arcade"
  codeLink="https://github.com/ArcadeAI/arcade-ai/tree/main/toolkits/teams"
  authType="OAuth2"
  versions={["0.1.0"]}
/>

<Badges repo="arcadeai/arcade_teams" />

The Teams toolkit provides a comprehensive set of tools for interacting with Microsoft Teams. Users can efficiently manage teams, channels, and chats, enabling them to:

- Retrieve information about users, teams, channels, and chats.
- List and search for users, teams, and channels within the organization.
- Send and retrieve messages in both channels and chats.
- Create new chats and manage channel memberships.

This toolkit streamlines collaboration and communication within Microsoft Teams, making it easier to connect with team members and access relevant information.

## Available Tools

<TableOfContents
  headers={["Tool Name", "Description"]}
  data={
    [
      ["Teams.GetSignedInUser", "Get the user currently signed in."],
      ["Teams.ListUsers", "List the users in the tenant."],
      ["Teams.SearchUsers", "List the users in the tenant."],
      ["Teams.GetChannel", "Retrieves metadata about a channel."],
      ["Teams.ListChannels", "Lists channels in a given team (including incoming channels shared with the team)."],
      ["Teams.SearchChannels", "Lists the channels in a given team."],
      ["Teams.GetPrimaryChannel", "The primary channel of a team."],
      ["Teams.ListChannelMembers", "Lists the members of a channel."],
      ["Teams.GetChannelMessages", "Gets the messages in a channel."],
      ["Teams.GetChannelMessageReplies", "Gets the replies to a Microsoft Teams channel message."],
      ["Teams.SendMessageToChannel", "Sends a message to a Microsoft Teams channel."],
      ["Teams.ListTeams", "Lists the teams the current user is associated with."],
      ["Teams.SearchTeams", "Search for teams in the organization (regardless of whether the current user is a member)."],
      ["Teams.GetTeam", "Retrieves metadata about a team."],
      ["Teams.ListTeamMembers", "Lists the members of a team."],
      ["Teams.SearchTeamMembers", "Lists the members of a team matching the search criteria."],
      ["Teams.GetChatMessages", "Retrieves messages from a Microsoft Teams chat."],
      ["Teams.GetChat", "Retrieves metadata about a Microsoft Teams chat."],
      ["Teams.ListChats", "List the Microsoft Teams chats to which the current user is a member of."],
      ["Teams.SendMessageToChat", "Sends a message to a Microsoft Teams chat."],
      ["Teams.CreateChat", "Creates a Microsoft Teams chat."],
      ["Teams.SearchPeople", "Search for people in the organization."],
      ["Teams.SearchMessages", "Searches for messages across Microsoft Teams chats and channels."],
    ]
  }
/>

<Tip>
  If you need to perform an action that's not listed here, you can [get in touch
  with us](mailto:contact@arcade.dev) to request a new tool, or [create your
  own tools](/home/build-tools/create-a-toolkit).
</Tip>

## Teams.GetSignedInUser

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_signed_in_user_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_signed_in_user_example_call_tool.js"],
      },
    },
  ]}
/>

Get the user currently signed in.

**Parameters**



## Teams.ListUsers

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/list_users_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/list_users_example_call_tool.js"],
      },
    },
  ]}
/>

List the users in the tenant.

**Parameters**

- **limit** (`integer`, optional) The maximum number of users to return. Defaults to 50, max is 100.
- **offset** (`integer`, optional) The offset to start from.


## Teams.SearchUsers

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/search_users_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/search_users_example_call_tool.js"],
      },
    },
  ]}
/>

List the users in the tenant.

**Parameters**

- **keywords** (`array[string]`, required) The keywords to match against users' names.
- **match_type** (`Enum` [PartialMatchType](/toolkits/social-communication/teams/reference#PartialMatchType), optional) The type of match to use for the keywords. Defaults to match_any_of_the_keywords.
- **limit** (`integer`, optional) The maximum number of users to return. Defaults to 50, max is 999.
- **offset** (`integer`, optional) The offset to start from.


## Teams.GetChannel

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_channel_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_channel_example_call_tool.js"],
      },
    },
  ]}
/>

Retrieves metadata about a channel.

**Parameters**

- **team_id_or_name** (`string`, optional) The ID or name of the team to get the channel of. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be returned with a list of all teams to pick from.
- **channel_id_or_name** (`string`, required) The ID or name of the channel to get.


## Teams.ListChannels

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/list_channels_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/list_channels_example_call_tool.js"],
      },
    },
  ]}
/>

Lists channels in a given team (including incoming channels shared with the team).

**Parameters**

- **team_id_or_name** (`string`, optional) The ID or name of the team to list the channels of. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be returned with a list of all teams to pick from.
- **limit** (`integer`, optional) The maximum number of channels to return. Defaults to 50, max is 100.
- **offset** (`integer`, optional) The offset to start from.


## Teams.SearchChannels

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/search_channels_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/search_channels_example_call_tool.js"],
      },
    },
  ]}
/>

Lists the channels in a given team.

**Parameters**

- **team_id_or_name** (`string`, optional) The ID or name of the team to list the channels of. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be raised with a list of available teams to pick from.
- **keywords** (`array[string]`, required) The keywords to search for in channel names.
- **match_type** (`Enum` [MatchType](/toolkits/social-communication/teams/reference#MatchType), optional) The type of match to use for the search. Defaults to 'partial_match_all_keywords'.
- **limit** (`integer`, optional) The maximum number of channels to return. Defaults to 50. Max of 100.
- **offset** (`integer`, optional) The offset to start from.


## Teams.GetPrimaryChannel

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_primary_channel_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_primary_channel_example_call_tool.js"],
      },
    },
  ]}
/>

The primary channel of a team.

**Parameters**

- **team_id_or_name** (`string`, optional) The ID or name of the team to get the primary channel. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be returned with a list of all teams to pick from.


## Teams.ListChannelMembers

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/list_channel_members_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/list_channel_members_example_call_tool.js"],
      },
    },
  ]}
/>

Lists the members of a channel.

**Parameters**

- **team_id_or_name** (`string`, optional) The ID or name of the team to list the members of. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be returned with a list of all teams to pick from.
- **channel_id_or_name** (`string`, optional) The ID or name of the channel to list the members of. If not provided: in case the team has a single channel, the tool will use it; otherwise an error will be returned with a list of all channels to pick from.
- **limit** (`integer`, optional) The maximum number of members to return. Defaults to 50, max is 999.
- **offset** (`integer`, optional) The offset to start from.


## Teams.GetChannelMessages

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_channel_messages_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_channel_messages_example_call_tool.js"],
      },
    },
  ]}
/>

Gets the messages in a channel.

**Parameters**

- **team_id_or_name** (`string`, optional) The ID or name of the team to get the messages of. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be returned with a list of all teams to pick from.
- **channel_id_or_name** (`string`, required) The ID or name of the channel to get the messages of.
- **limit** (`integer`, optional) The maximum number of messages to return. Defaults to 50, max is 50.


## Teams.GetChannelMessageReplies

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_channel_message_replies_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_channel_message_replies_example_call_tool.js"],
      },
    },
  ]}
/>

Gets the replies to a Microsoft Teams channel message.

**Parameters**

- **message_id** (`string`, required) The ID of the message to get the replies of.
- **team_id_or_name** (`string`, optional) The ID or name of the team to get the replies of. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be returned with a list of all teams to pick from.
- **channel_id_or_name** (`string`, required) The ID or name of the channel to get the replies of.


## Teams.SendMessageToChannel

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/send_message_to_channel_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/send_message_to_channel_example_call_tool.js"],
      },
    },
  ]}
/>

Sends a message to a Microsoft Teams channel.

**Parameters**

- **message** (`string`, required) The message to send to the channel.
- **team_id_or_name** (`string`, optional) The ID or name of the team to send the message to. If not provided: in case the user is a member of a single team, the tool will use it; otherwise an error will be returned with a list of all teams to pick from.
- **channel_id_or_name** (`string`, required) The ID or name of the channel to send the message to.


## Teams.ListTeams

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/list_teams_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/list_teams_example_call_tool.js"],
      },
    },
  ]}
/>

Lists the teams the current user is associated with.

**Parameters**

- **membership_type** (`Enum` [TeamMembershipType](/toolkits/social-communication/teams/reference#TeamMembershipType), optional) The type of membership to filter by. Defaults to 'direct_member_of_the_team'.


## Teams.SearchTeams

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/search_teams_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/search_teams_example_call_tool.js"],
      },
    },
  ]}
/>

Search for teams in the organization (regardless of whether the current user is a member).

**Parameters**

- **team_name_starts_with** (`string`, required) The prefix to match the name of the teams.
- **limit** (`integer`, optional) The maximum number of teams to return. Defaults to 10, max is 50.
- **next_page_token** (`string`, optional) The token to use to get the next page of results.


## Teams.GetTeam

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_team_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_team_example_call_tool.js"],
      },
    },
  ]}
/>

Retrieves metadata about a team.

**Parameters**

- **team_id** (`string`, optional) The ID of the team to get.
- **team_name** (`string`, optional) The name of the team to get. Prefer providing a team_id, when available, for optimal performance.


## Teams.ListTeamMembers

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/list_team_members_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/list_team_members_example_call_tool.js"],
      },
    },
  ]}
/>

Lists the members of a team.

**Parameters**

- **team_id** (`string`, optional) The ID of the team to list the members of.
- **team_name** (`string`, optional) The name of the team to list the members of. Prefer providing a team_id, when available, for optimal performance.
- **limit** (`integer`, optional) The maximum number of members to return. Defaults to 50, max is 999.
- **offset** (`integer`, optional) The number of members to skip. Defaults to 0.


## Teams.SearchTeamMembers

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/search_team_members_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/search_team_members_example_call_tool.js"],
      },
    },
  ]}
/>

Lists the members of a team matching the search criteria.

**Parameters**

- **member_name_starts_with** (`string`, required) The prefix to match the name of the members.
- **team_id** (`string`, optional) The ID of the team to list the members of.
- **team_name** (`string`, optional) The name of the team to list the members of. Prefer providing a team_id, when available, for optimal performance.
- **limit** (`integer`, optional) The maximum number of members to return. Defaults to 50, max is 100.
- **offset** (`integer`, optional) The number of members to skip. Defaults to 0.


## Teams.GetChatMessages

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_chat_messages_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_chat_messages_example_call_tool.js"],
      },
    },
  ]}
/>

Retrieves messages from a Microsoft Teams chat.

**Parameters**

- **chat_id** (`string`, optional) The ID of the chat to get messages from.
- **user_ids** (`array[string]`, optional) The IDs of the users in the chat to get messages from.
- **user_names** (`array[string]`, optional) The names of the users in the chat to get messages from. Prefer providing user_ids, when available, since the performance is better.
- **start_datetime** (`string`, optional) The start date to filter messages. Provide a string in the format 'YYYY-MM-DD' or 'YYYY-MM-DD HH:MM:SS'. Defaults to None (no start date filter).
- **end_datetime** (`string`, optional) The end date to filter messages. Provide a string in the format 'YYYY-MM-DD' or 'YYYY-MM-DD HH:MM:SS'. Defaults to None (no end date filter).
- **limit** (`integer`, optional) The maximum number of messages to return. Defaults to 50, max is 50.


## Teams.GetChat

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/get_chat_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/get_chat_example_call_tool.js"],
      },
    },
  ]}
/>

Retrieves metadata about a Microsoft Teams chat.

**Parameters**

- **chat_id** (`string`, optional) The ID of the chat to get metadata about.
- **user_ids** (`array[string]`, optional) The IDs of the users in the chat to get metadata about.
- **user_names** (`array[string]`, optional) The names of the users in the chat to get messages from. Prefer providing user_ids, when available, since the performance is better.


## Teams.ListChats

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/list_chats_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/list_chats_example_call_tool.js"],
      },
    },
  ]}
/>

List the Microsoft Teams chats to which the current user is a member of.

**Parameters**

- **limit** (`integer`, optional) The maximum number of chats to return. Defaults to 50, max is 50.
- **next_page_token** (`string`, optional) The token to use to get the next page of results.


## Teams.SendMessageToChat

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/send_message_to_chat_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/send_message_to_chat_example_call_tool.js"],
      },
    },
  ]}
/>

Sends a message to a Microsoft Teams chat.

**Parameters**

- **message** (`string`, required) The message to send to the chat.
- **chat_id** (`string`, optional) The ID of the chat to get messages from.
- **user_ids** (`array[string]`, optional) The IDs of the users in the chat to get messages from.
- **user_names** (`array[string]`, optional) The names of the users in the chat to get messages from. Prefer providing user_ids, when available, since the performance is better.


## Teams.CreateChat

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/create_chat_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/create_chat_example_call_tool.js"],
      },
    },
  ]}
/>

Creates a Microsoft Teams chat.

**Parameters**

- **user_ids** (`array[string]`, optional) The IDs of the users to create a chat with.
- **user_names** (`array[string]`, optional) The names of the users to create a chat with.


## Teams.SearchPeople

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/search_people_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/search_people_example_call_tool.js"],
      },
    },
  ]}
/>

Search for people in the organization.

**Parameters**

- **keywords** (`array[string]`, required) The keywords to match against people's names. Provide one or more expressions.
- **match_type** (`Enum` [PartialMatchType](/toolkits/social-communication/teams/reference#PartialMatchType), optional) The type of match to use for the keywords. Defaults to match_any_of_the_keywords.
- **limit** (`integer`, optional) The maximum number of people to return. Defaults to 50, max is 100.
- **next_page_token** (`string`, optional) The next page token to use for pagination.


## Teams.SearchMessages

<br />
<TabbedCodeBlock
  tabs={[
    {
      label: "Call the Tool Directly",
      content: {
        Python: ["/examples/integrations/toolkits/teams/search_messages_example_call_tool.py"],
        JavaScript: ["/examples/integrations/toolkits/teams/search_messages_example_call_tool.js"],
      },
    },
  ]}
/>

Searches for messages across Microsoft Teams chats and channels.

**Parameters**

- **keywords** (`string`, required) The keywords to match against messages' content.
- **limit** (`integer`, optional) The maximum number of messages to return. Defaults to 50, max is 50.
- **offset** (`integer`, optional) The offset to start from.




## Auth

The Arcade Teams toolkit uses the [Microsoft auth provider](/home/auth-providers/microsoft) to connect to users' Teams accounts. Please refer to the [Microsoft auth provider](/home/auth-providers/microsoft) documentation to learn how to configure auth.

<ToolFooter pipPackageName="arcade_teams" />

