---
title: "Call tools directly"
description: "Learn how to call tools programmatically using Arcade"
---

import { Steps, Tabs } from "nextra/components";
import { SignupLink } from "@/components/Analytics";

# Call Tools Directly

Direct tool calling is useful in situations where you already know what tool is needed or when there is no prompt for an LLM to interpret and decide on the tool calling. It also gives you full control over what is executed by the Arcade Engine and how.

<Steps>

### Prerequisites

1. Create an <SignupLink utmParams={{
            utm_source: "docs",
            utm_medium: "page",
            utm_campaign: "call-tools-directly",
          }} linkLocation="docs:call-tools-directly">Arcade account</SignupLink>
1. Get an [Arcade API key](/home/api-keys) and take note, you'll need it in the next steps.

### Install the Arcade client

<Tabs items={["Python", "JavaScript"]}>
<Tabs.Tab>
```python
pip install arcadepy
```
</Tabs.Tab>
<Tabs.Tab>
```javascript
npm install @arcadeai/arcadejs
```
</Tabs.Tab>
</Tabs>

### Instantiate the client

<Tabs items={["Python", "JavaScript"]}>
<Tabs.Tab>
Create a new script called `call_tools_directly.py` and instantiate the Arcade client:
```python file=<rootDir>/examples/code/home/use-tools/call-tools-directly/math_sqrt_directly.py#L1-L3
```
</Tabs.Tab>
<Tabs.Tab>
Create a new script called `call_tools_directly.js` and instantiate the Arcade client:
```javascript file=<rootDir>/examples/code/home/use-tools/call-tools-directly/math_sqrt_directly.js#L1-L3
```
</Tabs.Tab>
</Tabs>

<Note>
You can also set the `ARCADE_API_KEY` environment variable instead of passing it as a parameter.
</Note>

### Set the user ID

Arcade needs a unique identifier for your application user (this could be an email address, a UUID, etc). In this example, simply use your email address as the user ID:

<Tabs items={["Python", "JavaScript"]}>
<Tabs.Tab>
```python file=<rootDir>/examples/code/home/use-tools/call-tools-directly/math_sqrt_directly.py#L5-L5
```
</Tabs.Tab>
<Tabs.Tab>
```js file=<rootDir>/examples/code/home/use-tools/call-tools-directly/math_sqrt_directly.js#L5-L5
```
</Tabs.Tab>
</Tabs>

### Call a tool

Let's use the `Math.Sqrt` tool from the Arcade Math toolkit to get the square root of a number.

<Tabs items={["Python", "JavaScript"]}>
<Tabs.Tab>
```python file=<rootDir>/examples/code/home/use-tools/call-tools-directly/math_sqrt_directly.py#L7-L17
```
</Tabs.Tab>
<Tabs.Tab>
```js file=<rootDir>/examples/code/home/use-tools/call-tools-directly/math_sqrt_directly.js#L7-L17
```
</Tabs.Tab>
</Tabs>

### Run the code

<Tabs items={["Python", "JavaScript"]}>
<Tabs.Tab>
```bash
python3 example.py
> 25
```
</Tabs.Tab>
<Tabs.Tab>
```bash
node example.js
> 25
```
</Tabs.Tab>
</Tabs>

</Steps>

## Next Steps

This was a very simple tool call. Try executing a [tool with user authorization](/home/auth/auth-tool-calling) next.
