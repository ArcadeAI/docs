import type { MetaRecord } from "nextra";

export const meta: MetaRecord = {
  // Categories link to /resources/integrations/* for now
  // This is a landing page that clarifies these are ready-to-use tools
};

export default meta;
