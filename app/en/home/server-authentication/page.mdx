---
title: Server Authentication
description: Registry of all server authentication providers available in the Arcade ecosystem
---

import { ToolCard } from "@/app/_components/tool-card";

# Server Authentication

Secure your MCP server with one of the following authentication patterns. For specific integrations, see the [Server Auth Integrations](/home/server-auth-integrations) page.


<br />

<div className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-2">
  <ToolCard
    name="JWT Verification"
    image="mcp"
    summary="Validate bearer tokens from authorization servers without handling user authentication flows"
    link="/home/server-auth-providers/jwt-verifier"
  />
  <ToolCard
    name="Remote OAuth"
    image="mcp"
    summary="Connect to an external authorization server that supports Dynamic Client Registration (DCR)"
    link="/home/server-auth-providers/remote-auth"
  />
  <ToolCard
    name="Client ID Metadata Documents (CIMD)"
    image="mcp"
    summary="Configure OAuth and OIDC providers using Client ID Metadata Documents"
    link="/home/server-auth-providers/oidc-proxy"
    comingSoon={true}
  />
  <ToolCard
    name="OIDC Proxy"
    image="mcp"
    summary="Connect to an external OIDC provider that doesn't support Dynamic Client Registration (DCR)"
    link="/home/server-auth-providers/oidc-proxy"
    comingSoon={true}
  />
  <ToolCard
    name="OAuth Proxy"
    image="mcp"
    summary="Connect to an external OAuth provider that doesn't support Dynamic Client Registration (DCR)"
    link="/home/server-auth-providers/oauth-proxy"
    comingSoon={true}
  />
</div>
