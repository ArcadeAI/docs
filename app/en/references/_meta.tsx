import type { MetaRecord } from "nextra";

export const meta: MetaRecord = {
  "*": {
    theme: {
      breadcrumb: false,
      layout: "full",
      toc: false,
      copyPage: false,
    },
  },
};

export default meta;
