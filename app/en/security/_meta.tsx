import type { MetaRecord } from "nextra";

export const meta: MetaRecord = {
  // Security guides are linked from the landing page
  // This section provides a unified entry point for security topics
};

export default meta;
