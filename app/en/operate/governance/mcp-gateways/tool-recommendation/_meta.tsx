import type { MetaRecord } from "nextra";

export const meta: MetaRecord = {
  configure: {
    title: "Configure",
  },
  limitations: {
    title: "When not to use it",
  },
};

export default meta;
